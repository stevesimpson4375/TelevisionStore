@model IEnumerable<TelevisionStore.Models.ItemList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SaleId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TelevisionId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SaleId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TelevisionId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemListId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemListId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemListId })
        </td>
    </tr>
}

</table>
